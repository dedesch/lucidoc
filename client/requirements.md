## Packages
framer-motion | Smooth animations for chat bubbles and page transitions
react-markdown | Rendering markdown content in AI responses
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind CSS classes safely

## Notes
Authentication uses session cookies (credentials: "include")
Chat interface requires auto-scrolling to bottom
AI responses may include markdown formatting
